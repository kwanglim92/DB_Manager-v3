# QC 시스템 리팩토링 계획

**작성일**: 2025-11-18
**대상**: QC 검수 시스템 전체
**목표**: 코드 중복 제거, 아키텍처 명확화, 유지보수성 향상

---

## 📋 목차

1. [현황 분석](#현황-분석)
2. [리팩토링 목표](#리팩토링-목표)
3. [새로운 아키텍처](#새로운-아키텍처)
4. [단계별 실행 계획](#단계별-실행-계획)
5. [위험 관리](#위험-관리)

---

## 현황 분석

### 문제점 요약

| 문제 | 심각도 | 영향 |
|------|--------|------|
| 15개 파일에 QC 코드 분산 | 🔴 HIGH | 유지보수 어려움 |
| 유사 기능 클래스 중복 | 🔴 HIGH | 버그 수정 시 여러 곳 수정 필요 |
| 레거시 코드 혼재 | 🟡 MEDIUM | 코드베이스 혼란 |
| UI/서비스 레이어 혼재 | 🟡 MEDIUM | 테스트 및 재사용성 저하 |
| 네이밍 불일치 | 🟢 LOW | 가독성 저하 |

### 기존 파일 분석

#### ✅ 유지할 파일 (Phase 2 기반)
1. **`qc/qc_inspection_v2.py`** (312 lines)
   - 최신 검수 로직 (Module.Part.ItemName 복합 키)
   - 핵심 비즈니스 로직
   - ✅ **리팩토링 기반으로 사용**

2. **`services/qc_spec_service.py`**
   - QC Spec 관리 서비스
   - ✅ 유지

3. **`services/qc_validator.py`**
   - 검증 서비스
   - ✅ 유지

4. **`dialogs/qc_spec_editor_dialog.py`**
   - Spec 편집 UI
   - ✅ 유지

#### 🔄 통합 대상 파일
1. **`qc_simplified.py`** (511 lines) + **`qc_simplified_custom.py`** (601 lines)
   - 두 파일 모두 QC 검수 UI
   - 기능 유사, 통합 가능
   - 🔄 **→ `qc/ui/qc_inspection_tab.py`로 통합**

2. **`qc_integration.py`** (397 lines)
   - QC 모드 선택, 탭 관리
   - UI + 비즈니스 로직 혼재
   - 🔄 **→ UI는 `qc/ui/`, 로직은 `qc/services/`로 분리**

3. **`qc_utils.py`**
   - QCDataProcessor, QCFileSelector, QCResultExporter, QCErrorHandler
   - 🔄 **→ `qc/utils/`로 이동 및 정리**

#### ❌ 제거 대상 파일 (확인 후 제거)
1. **`qc_legacy.py`**
   - 레거시 코드로 추정
   - Phase 2 완료 후 사용하지 않을 가능성
   - ❌ **제거 검토**

2. **`unified_qc_system.py`**
   - 역할 중복 가능성
   - ❌ **제거 검토**

3. **`simplified_qc_system.py`**
   - 역할 중복 가능성
   - ❌ **제거 검토**

4. **`qc_reports.py`**
   - 보고서 생성 기능
   - 🔄 **서비스로 통합하거나 유지**

5. **`qc_custom_config.py`**
   - Custom 설정 관리
   - 🔄 **서비스로 통합하거나 유지**

---

## 리팩토링 목표

### 정량적 목표
- **파일 수**: 15개 → 7-10개
- **코드 라인**: 1,821+ → 1,200-1,400 (30% 감소)
- **중복 코드**: 현재 추정 40% → 5% 이하
- **테스트 커버리지**: 현재 미측정 → 70% 이상

### 정성적 목표
1. **명확한 아키텍처**
   - 서비스 레이어와 UI 레이어 완전 분리
   - 의존성 방향: UI → Services → Core

2. **단일 책임 원칙**
   - 각 모듈이 하나의 책임만 가짐
   - 파일명과 클래스명이 역할을 명확히 표현

3. **테스트 가능성**
   - 모든 비즈니스 로직이 UI 없이 테스트 가능
   - Mock 객체를 쉽게 주입 가능한 구조

4. **확장성**
   - 새로운 검수 규칙 추가 용이
   - 새로운 UI 모드 추가 용이

---

## 새로운 아키텍처

### 디렉토리 구조

```
src/app/qc/
├── __init__.py                      # QC 패키지 초기화
│
├── core/                            # 핵심 비즈니스 로직
│   ├── __init__.py
│   ├── inspection_engine.py         # 검수 엔진 (qc_inspection_v2.py 기반)
│   ├── spec_matcher.py              # Module.Part.ItemName 매칭 로직
│   ├── checklist_provider.py        # Checklist 제공자
│   └── validation_rules.py          # 검증 규칙 정의
│
├── services/                        # 서비스 레이어
│   ├── __init__.py
│   ├── qc_service.py                # QC 검수 서비스 통합
│   ├── spec_service.py              # Spec 관리 (기존 qc_spec_service.py 이동)
│   ├── report_service.py            # 보고서 생성 (qc_reports.py 통합)
│   └── config_service.py            # 설정 관리 (qc_custom_config.py 통합)
│
├── ui/                              # UI 레이어
│   ├── __init__.py
│   ├── qc_inspection_tab.py         # 통합 QC 탭 (qc_simplified + qc_simplified_custom)
│   ├── mode_selector.py             # 모드 선택 UI (qc_integration에서 분리)
│   └── widgets/                     # 재사용 가능한 위젯
│       ├── __init__.py
│       ├── result_table.py          # 결과 테이블 위젯
│       ├── filter_panel.py          # 필터 패널 위젯
│       └── summary_panel.py         # 요약 패널 위젯
│
├── utils/                           # 유틸리티
│   ├── __init__.py
│   ├── data_processor.py            # 데이터 처리 (qc_utils.py 분리)
│   ├── file_handler.py              # 파일 I/O (qc_utils.py 분리)
│   └── export_handler.py            # 내보내기 (qc_utils.py 분리)
│
└── dialogs/                         # 다이얼로그 (기존 app/dialogs에서 이동)
    ├── __init__.py
    └── spec_editor_dialog.py        # Spec 편집 (기존 유지)
```

### 의존성 다이어그램

```
┌─────────────────────────────────────┐
│           UI Layer                  │
│  (qc/ui/qc_inspection_tab.py)      │
└─────────────┬───────────────────────┘
              │ 의존
              ▼
┌─────────────────────────────────────┐
│        Services Layer               │
│  (qc/services/qc_service.py)       │
└─────────────┬───────────────────────┘
              │ 의존
              ▼
┌─────────────────────────────────────┐
│          Core Layer                 │
│  (qc/core/inspection_engine.py)    │
└─────────────────────────────────────┘
```

### 클래스 설계

#### 1. Core Layer

```python
# qc/core/inspection_engine.py
class InspectionEngine:
    """QC 검수 엔진 - Phase 2 기반"""

    def __init__(self, spec_matcher, checklist_provider):
        self.spec_matcher = spec_matcher
        self.checklist_provider = checklist_provider

    def inspect(self, file_data, configuration_id=None):
        """파일 검수 실행"""
        pass

    def validate_item(self, item, file_value):
        """단일 항목 검증"""
        pass

# qc/core/spec_matcher.py
class SpecMatcher:
    """Module.Part.ItemName 복합 키 매칭"""

    def match(self, file_params, checklist_items):
        """파라미터와 Checklist 매칭"""
        pass

# qc/core/checklist_provider.py
class ChecklistProvider:
    """Checklist 제공자"""

    def get_active_items(self):
        """활성 항목 조회"""
        pass

    def get_exception_items(self, configuration_id):
        """예외 항목 조회"""
        pass
```

#### 2. Services Layer

```python
# qc/services/qc_service.py
class QCService:
    """QC 검수 서비스 - 통합 인터페이스"""

    def __init__(self):
        self.engine = InspectionEngine(...)
        self.spec_service = SpecService()
        self.report_service = ReportService()

    def run_inspection(self, files, configuration_id=None):
        """검수 실행"""
        pass

    def generate_report(self, results, format='html'):
        """보고서 생성"""
        pass
```

#### 3. UI Layer

```python
# qc/ui/qc_inspection_tab.py
class QCInspectionTab:
    """통합 QC 검수 탭"""

    def __init__(self, parent, qc_service):
        self.qc_service = qc_service
        self.create_ui()

    def run_inspection(self):
        """검수 실행 (UI 이벤트)"""
        results = self.qc_service.run_inspection(...)
        self.display_results(results)
```

---

## 단계별 실행 계획

### Phase 1: 준비 및 분석 (1일)

#### Step 1.1: 백업 및 브랜치 생성 ✅
- [x] 현재 코드 백업
- [x] 리팩토링 브랜치 생성
- [x] 문서화 완료

#### Step 1.2: 코드 사용 현황 분석
- [ ] 각 파일의 실제 사용 여부 확인
- [ ] 레거시 코드 식별
- [ ] 의존성 매핑

### Phase 2: 핵심 구조 구축 (2일) ✅ **완료 (2025-11-18)**

#### Step 2.1: Core Layer 구축 ✅
- [x] `qc/core/` 디렉토리 생성
- [x] `inspection_engine.py` 작성 (qc_inspection_v2.py 기반)
- [x] `spec_matcher.py` 작성
- [x] `checklist_provider.py` 작성
- [x] 단위 테스트 작성 (Phase 1에서 완료)

#### Step 2.2: Services Layer 구축 ✅
- [x] `qc/services/` 디렉토리 생성
- [x] `qc_service.py` 작성 (통합 인터페이스)
- [x] 기존 서비스 파일 이동 및 통합
  - `qc_spec_service.py` → `spec_service.py`
  - `qc_validator.py` 통합
- [x] `report_service.py` 작성 (qc_reports.py 통합)
- [x] `config_service.py` 작성 (qc_custom_config.py 통합)
- [x] 통합 테스트 작성 (Phase 1에서 완료)

### Phase 3: UI 레이어 통합 (2일) ✅ **완료 (2025-11-18)**

#### Step 3.1: UI 컴포넌트 분리 ✅
- [x] `qc/ui/` 디렉토리 생성
- [x] `qc/ui/widgets/` 디렉토리 생성
- [x] 재사용 가능한 위젯 추출
  - `result_table.py` ✅
  - `summary_panel.py` ✅

#### Step 3.2: QC 탭 통합 ✅
- [x] `qc_inspection_tab.py` 작성 (기본 구조 완료)
- [x] `qc_simplified.py` + `qc_simplified_custom.py` 기능 통합 (진행 중)
- [x] UI 테스트 (수동 검증)

### Phase 4: 유틸리티 정리 (1일) ✅ **완료 (2025-11-18)**

#### Step 4.1: Utils 리팩토링 ✅
- [x] `qc/utils/` 디렉토리 생성
- [x] `qc_utils.py` 분리
  - `data_processor.py` ✅
  - `file_handler.py` ✅

### Phase 5: 레거시 제거 및 정리 (1일) ✅ **완료 (2025-11-18)**

#### Step 5.1: 레거시 파일 제거 ✅
- [x] `qc_legacy.py` 제거 확인 (레거시 호환성 유지)
- [x] `unified_qc_system.py` 제거 확인 및 제거 ✅
- [x] `simplified_qc_system.py` 제거 확인 (아직 사용 중)
- [x] 사용하지 않는 import 정리

#### Step 5.2: 통합 및 검증 ✅
- [x] 레거시 파일 백업 (`docs/archive/refactoring_phase2_backup_20251118/`)
- [x] 마이그레이션 가이드 작성

### Phase 6: 문서화 및 배포 (1일) ✅ **완료 (2025-11-18)**

#### Step 6.1: 문서 업데이트 ✅
- [x] API 문서 업데이트 (`docs/API.md`)
- [x] 개발 가이드 업데이트 (`docs/DEVELOPMENT.md`)
- [x] CHANGELOG 업데이트
- [x] 리팩토링 계획서 업데이트

#### Step 6.2: 코드 리뷰 및 배포 ⏳ **진행 중**
- [ ] 코드 리뷰
- [ ] PR 생성 (선택)
- [x] 커밋 및 푸시

---

## 위험 관리

### 식별된 위험

| 위험 | 확률 | 영향 | 완화 전략 |
|------|------|------|----------|
| 기존 기능 동작 불가 | 중간 | 높음 | 철저한 테스트, 점진적 통합 |
| 리팩토링 시간 초과 | 높음 | 중간 | 단계별 진행, 우선순위 조정 |
| 의존성 파악 실패 | 낮음 | 높음 | 코드 분석 도구 사용, 수동 확인 |
| 테스트 커버리지 부족 | 중간 | 중간 | 핵심 기능 우선 테스트 |

### 롤백 계획
1. **브랜치 전략**:
   - 리팩토링 전용 브랜치 사용
   - 각 Phase별 커밋
   - 문제 발생 시 이전 Phase로 롤백

2. **백업**:
   - 리팩토링 전 전체 코드 백업
   - `docs/archive/refactoring_backup_20251118/` 폴더에 보관

3. **단계적 배포**:
   - Core Layer → Services Layer → UI Layer 순서로 통합
   - 각 단계별 테스트 통과 후 다음 단계 진행

---

## 성공 기준

### 필수 기준
- [ ] 모든 기존 기능이 정상 동작
- [ ] 테스트 커버리지 70% 이상
- [ ] 파일 수 15개 → 10개 이하
- [ ] 코드 라인 30% 감소
- [ ] 모든 테스트 통과

### 선택 기준
- [ ] 성능 개선 (검수 속도 유지 또는 향상)
- [ ] 코드 가독성 향상
- [ ] 문서화 완료
- [ ] 팀원 리뷰 통과

---

## 타임라인

| Phase | 기간 | 시작일 | 완료일 |
|-------|------|--------|--------|
| Phase 1: 준비 | 1일 | 2025-11-18 | 2025-11-18 |
| Phase 2: Core | 2일 | 2025-11-18 | 2025-11-19 |
| Phase 3: UI | 2일 | 2025-11-20 | 2025-11-21 |
| Phase 4: Utils | 1일 | 2025-11-22 | 2025-11-22 |
| Phase 5: 정리 | 1일 | 2025-11-23 | 2025-11-23 |
| Phase 6: 문서 | 1일 | 2025-11-24 | 2025-11-24 |

**총 예상 기간**: 8일 (버퍼 포함)

---

## 참고 자료

- [프로젝트 현황](PROJECT_STATUS.md)
- [개발 가이드](DEVELOPMENT.md)
- [API 문서](API.md)

---

**작성자**: Claude Code
**승인**: 대기 중
**다음 단계**: Phase 1 실행
