# 📋 QC 검수 탭 UI 재편성 계획

## 🎯 목표
QC 스펙 관리(QC_Spec_Master)에 있는 항목들만 단순 비교하는 간소화된 UI 구현

## 📊 현재 상태 분석

### 제거할 요소들
1. **QC Mode Configuration 섹션**
   - ❌ Inspection Mode 라디오 버튼 (Check List Focused / Full Inspection)
   - ❌ 복잡한 모드 선택 로직

2. **QC Execution Control 섹션**  
   - ❌ Select QC Files 버튼 (파일 선택은 유지하되 단순화)
   - ❌ Execute QC Inspection 버튼 (자동 실행으로 변경)
   - ❌ Export to Excel 버튼 (결과 섹션으로 이동)

3. **QC Inspection Results 섹션**
   - ❌ 복잡한 트리뷰 구조
   - ❌ Issue Type 컬럼
   - ❌ Description 컬럼 (필요시 툴팁으로 대체)

4. **QC Statistics & Analysis 섹션**
   - ❌ 복잡한 통계 (단순 Pass/Fail 카운트만 유지)

## 🏗️ 새로운 UI 구조

### 1. 상단 제어 패널 (간소화)
```
┌─────────────────────────────────────────────────────────┐
│ 🔍 QC 검수 - 단순 스펙 비교                              │
├─────────────────────────────────────────────────────────┤
│ Equipment Type: [드롭다운 v]  📁 파일 선택  🔄 새로고침   │
└─────────────────────────────────────────────────────────┘
```

### 2. 메인 비교 영역
```
┌─────────────────────────────────────────────────────────┐
│ 📊 검수 결과                                             │
├────────────┬────────────┬──────────┬──────────┬────────┤
│ Item Name  │ 측정값      │ Min Spec │ Max Spec │ 결과    │
├────────────┼────────────┼──────────┼──────────┼────────┤
│ Temperature│ 23.5       │ 20       │ 25       │ ✅ Pass │
│ Pressure   │ 250        │ 100      │ 200      │ ❌ Fail │
│ Flow Rate  │ 15.2       │ 10       │ 20       │ ✅ Pass │
└────────────┴────────────┴──────────┴──────────┴────────┘
```

### 3. 하단 요약 패널
```
┌─────────────────────────────────────────────────────────┐
│ 📈 검수 요약                                             │
├─────────────────────────────────────────────────────────┤
│ Total: 50 | Pass: 45 (90%) | Fail: 5 (10%)            │
│ [📥 결과 내보내기]                                        │
└─────────────────────────────────────────────────────────┘
```

## 🔧 구현 상세

### Phase 1: UI 요소 제거 (즉시 실행)
```python
def simplify_qc_tab_ui(self):
    """QC 검수 탭 UI 간소화"""
    # 1. QC Mode Configuration 제거
    if hasattr(self, 'qc_mode_frame'):
        self.qc_mode_frame.destroy()
    
    # 2. 복잡한 버튼들 제거
    if hasattr(self, 'execute_qc_btn'):
        self.execute_qc_btn.destroy()
    
    # 3. 불필요한 통계 패널 제거
    if hasattr(self, 'statistics_frame'):
        self.statistics_frame.destroy()
```

### Phase 2: 새로운 UI 생성
```python
def create_simplified_qc_tab(self):
    """간소화된 QC 검수 탭 생성"""
    
    # 1. 제어 패널 (한 줄로 간소화)
    control_panel = ttk.Frame(self.qc_check_frame)
    control_panel.pack(fill=tk.X, padx=10, pady=5)
    
    # Equipment Type 선택
    ttk.Label(control_panel, text="Equipment Type:").pack(side=tk.LEFT)
    self.qc_equipment_combo = ttk.Combobox(control_panel, width=30)
    self.qc_equipment_combo.pack(side=tk.LEFT, padx=5)
    
    # 파일 선택 버튼 (간단)
    ttk.Button(control_panel, text="📁 파일 선택", 
              command=self.select_qc_files).pack(side=tk.LEFT, padx=5)
    
    # 새로고침 버튼
    ttk.Button(control_panel, text="🔄 새로고침",
              command=self.refresh_qc_results).pack(side=tk.LEFT)
    
    # 2. 결과 테이블 (간소화)
    self.create_simple_results_table()
    
    # 3. 요약 패널
    self.create_summary_panel()
```

### Phase 3: 데이터 처리 로직 간소화
```python
def perform_simple_qc_check(self, file_data):
    """단순 QC 검수 실행"""
    results = []
    
    # QC_Spec_Master에서 스펙 가져오기
    specs = self.qc_spec_service.get_all_specs()
    
    for item_name, measured_value in file_data.items():
        # ItemName 매칭으로 스펙 찾기
        spec = self.find_spec_by_item_name(item_name)
        if spec:
            result = {
                'item_name': item_name,
                'measured': measured_value,
                'min_spec': spec.get('min_spec'),
                'max_spec': spec.get('max_spec'),
                'result': self.check_pass_fail(measured_value, spec)
            }
            results.append(result)
    
    return results
```

## 📉 제거 목록 (상세)

### 완전 제거
- ❌ Check List Focused / Full Inspection 모드
- ❌ QC Checklist 관련 모든 기능
- ❌ Performance Item 구분
- ❌ Issue Type 분류
- ❌ 복잡한 트리뷰 계층 구조
- ❌ Configuration별 스펙 관리

### 단순화
- ✅ 파일 선택 → 단일 버튼으로
- ✅ 검수 실행 → 파일 선택 시 자동 실행
- ✅ 결과 표시 → 단순 테이블
- ✅ 통계 → Pass/Fail 카운트만

## 🎯 기대 효과

### 사용성 개선
- **클릭 수 감소**: 5-6번 → 2번
- **화면 복잡도**: 70% 감소
- **학습 시간**: 30분 → 5분

### 유지보수 개선
- **코드 라인**: 약 60% 감소
- **버그 가능성**: 현저히 감소
- **테스트 시간**: 80% 감소

## 📅 구현 순서

### Step 1: 백업 (5분)
```bash
cp src/app/manager.py src/app/manager_backup.py
git add . && git commit -m "backup: QC 탭 재편성 전 백업"
```

### Step 2: 기존 UI 제거 (10분)
- QC Mode Configuration 프레임 제거
- 복잡한 버튼들 제거
- 불필요한 통계 패널 제거

### Step 3: 새 UI 구현 (20분)
- 간소화된 제어 패널
- 단순 결과 테이블
- 기본 요약 패널

### Step 4: 로직 연결 (15분)
- QC_Spec_Master 연동
- 파일 데이터 처리
- 결과 표시

### Step 5: 테스트 (10분)
- 파일 선택 테스트
- 결과 표시 확인
- 내보내기 기능 확인

## 💡 추가 개선 아이디어

### 자동화
- 폴더 감시 → 새 파일 자동 검수
- 결과 자동 저장
- 이메일 알림 (Fail 발생 시)

### 시각화
- Pass/Fail 차트
- 트렌드 그래프 (선택적)
- 히트맵 (문제 영역 표시)

### 통합
- Default DB와 직접 연동
- 실시간 스펙 업데이트
- 이력 관리

---
*작성일: 2025-11-17*
*목적: QC 검수 탭 간소화 및 사용성 개선*