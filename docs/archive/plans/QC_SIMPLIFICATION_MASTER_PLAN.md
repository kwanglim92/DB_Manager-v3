# 📋 QC 검수 탭 간소화 - 마스터 플랜

## 🎯 프로젝트 개요

### 배경
- 현재 QC 검수 탭이 너무 복잡함 (Check List 모드, Full Inspection 등)
- QC_Spec_Master 기반 단순 비교만 필요
- 사용자 요구: 간단하고 직관적인 UI

### 목표
- QC 스펙 관리 항목들만 단순 비교
- 클릭 수 최소화 (6회 → 2회)
- 학습 시간 단축 (30분 → 5분)

## 🏗️ 아키텍처

### 기존 UI (제거 예정)
```
QC Mode Configuration
├── Check List Focused
├── Full Inspection
└── Complex Statistics

QC Execution Control
├── Select Files
├── Execute Inspection
└── Export Results
```

### 새 UI (간소화)
```
Control Panel (1줄)
├── Equipment Type 선택
├── 파일 선택 (자동 실행)
└── 새로고침

Results Table
├── Item Name
├── 측정값
├── Min/Max Spec
└── Pass/Fail

Summary
├── Total/Pass/Fail Count
└── Export Button
```

## 📅 개발 일정 (8주)

### Phase 1: 프로토타입 (Week 1) ✅
- [x] 독립 모듈 생성 (`test_simplified_qc_ui.py`)
- [x] 기본 UI 구현
- [x] 샘플 데이터로 테스트

### Phase 2: 통합 준비 (Week 2)
- [ ] 실제 QC_Spec_Master 연동
- [ ] 파일 파싱 로직 구현
- [ ] DB 연결

### Phase 3: A/B 테스트 (Week 3-4)
- [ ] 메인 앱에 통합
- [ ] 두 버전 병행 운영
- [ ] 사용 통계 수집

### Phase 4: 피드백 (Week 5-6)
- [ ] 사용자 피드백 수집
- [ ] 개선사항 반영
- [ ] 버그 수정

### Phase 5: 전환 (Week 7-8)
- [ ] 점진적 전환
- [ ] 레거시 코드 제거
- [ ] 문서 업데이트

## 🔧 구현 상세

### 1. 데이터 구조
```python
# QC 스펙 (QC_Spec_Master)
{
    'item_name': str,      # ItemName (매칭 키)
    'min_spec': float,     # 최소 스펙
    'max_spec': float,     # 최대 스펙
    'unit': str,          # 단위
    'category': str       # 카테고리
}

# 검수 결과
{
    'item_name': str,      # 항목명
    'measured': float,     # 측정값
    'min_spec': float,     # 최소 스펙
    'max_spec': float,     # 최대 스펙
    'result': str         # Pass/Fail
}
```

### 2. 핵심 로직
```python
def perform_qc_check(file_data, specs):
    """단순 QC 검수"""
    results = []
    for item_name, value in file_data.items():
        if item_name in specs:
            spec = specs[item_name]
            pass_fail = spec['min'] <= value <= spec['max']
            results.append({
                'item_name': item_name,
                'measured': value,
                'result': 'Pass' if pass_fail else 'Fail'
            })
    return results
```

### 3. UI 컴포넌트

#### Control Panel
- Equipment Type: 콤보박스
- File Select: 버튼 (파일 다이얼로그)
- Refresh: 버튼 (재실행)

#### Results Table
- TreeView with 5 columns
- Color coding (Pass=Green, Fail=Red)
- Sortable columns

#### Summary Panel
- Statistics label
- Export button
- Filter checkbox

## 📊 성공 지표

### 정량적 지표
| 항목 | 현재 | 목표 | 개선율 |
|------|------|------|--------|
| 클릭 수 | 6회 | 2회 | 67% ⬇️ |
| 처리 시간 | 30초 | 15초 | 50% ⬇️ |
| 학습 시간 | 30분 | 5분 | 83% ⬇️ |
| 코드 라인 | 2000 | 800 | 60% ⬇️ |

### 정성적 지표
- 직관적인 UI
- 최소한의 사용자 입력
- 명확한 결과 표시
- 쉬운 데이터 내보내기

## 🧪 테스트 계획

### 단위 테스트
- [ ] 파일 로딩
- [ ] 스펙 매칭
- [ ] Pass/Fail 판정
- [ ] 결과 내보내기

### 통합 테스트
- [ ] End-to-End 플로우
- [ ] DB 연동
- [ ] 대용량 파일
- [ ] 동시성

### 사용자 테스트
- [ ] UI 직관성
- [ ] 성능 체감
- [ ] 오류 처리
- [ ] 피드백 수집

## 🚦 리스크 관리

### 기술적 리스크
| 리스크 | 영향도 | 대응 방안 |
|--------|--------|-----------|
| DB 연동 실패 | 높음 | 폴백 모드 준비 |
| 성능 저하 | 중간 | 캐싱 적용 |
| 호환성 문제 | 낮음 | 버전 체크 |

### 사용자 리스크
| 리스크 | 영향도 | 대응 방안 |
|--------|--------|-----------|
| 학습 저항 | 중간 | 교육 자료 제공 |
| 기능 부족 | 낮음 | 점진적 추가 |
| 데이터 손실 | 높음 | 자동 백업 |

## 📝 체크리스트

### 개발 전
- [x] 요구사항 분석
- [x] UI 설계
- [x] 계획 수립
- [x] 프로토타입 작성

### 개발 중
- [ ] QC_Spec_Master 연동
- [ ] 파일 파싱 구현
- [ ] UI 통합
- [ ] 테스트 작성

### 개발 후
- [ ] 사용자 테스트
- [ ] 문서 작성
- [ ] 배포 준비
- [ ] 모니터링

## 🔗 관련 문서
- [QC_TAB_UI_REDESIGN_PLAN.md](./QC_TAB_UI_REDESIGN_PLAN.md) - 초기 재설계 계획
- [QC_TAB_PHASED_TRANSITION_PLAN.md](./QC_TAB_PHASED_TRANSITION_PLAN.md) - 단계별 전환 계획
- [test_simplified_qc_ui.py](../test_simplified_qc_ui.py) - 프로토타입 코드

## 📌 현재 상태
- **Phase**: 1 (프로토타입 완료)
- **다음 단계**: Phase 2 (통합 준비)
- **예상 완료일**: 2025-01-12 (8주 후)

---
*최종 업데이트: 2025-11-17*
*담당: GenSpark AI Developer*